@Library('todoapp') _

todoapp (
        COMPONENT             : 'users',
        PROJECT_NAME          : "todo",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "JAVA"
)